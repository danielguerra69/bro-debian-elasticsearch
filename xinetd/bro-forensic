service bro
{
        socket_type     = stream
        wait            = no
        user            = root
        group           = root
        server          = /scripts/bro-forensic.sh
        server_args	    = FORENSICCONF
        instances       = 300
}
